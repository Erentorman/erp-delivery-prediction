## .NET
bin/
obj/
*.user

## IDE
.vs/
.idea/
*.swp

## macOS
**/.DS_Store

## Secrets — never commit the real env file
.env

## Docker
docker-compose.override.yml

## Python
**/.venv/
venv/
__pycache__/
.coverage
.coverage.*
htmlcov/
*.pyc
**/.pytest_cache/

## ERP seed converter — local source/generated files, not committed
tools/erp-seed-converter/*.xlsx
tools/erp-seed-converter/*.xls
tools/erp-seed-converter/output/
tools/erp-seed-converter/generated/

## AI training dataset preparation — generated output, not committed
ai-prediction/training/output/

## AI model training (T-907) — ephemeral training-run staging output, not
## the canonical runtime artifact (that is ai-prediction/model/artifacts/,
## tracked — populated only via training/promote_artifact.py)
ai-prediction/training/artifacts/

## Raw ERP source data — never committed (see tools/erp-seed-converter/README.md)
ai-prediction/data/
